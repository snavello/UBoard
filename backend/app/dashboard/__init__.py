"""Dashboard: el SpecDashboard (esquema.py), su validacion contra el modelo
(validacion.py), su versionado (operaciones.py), la traduccion de filtros
activos a filtros de consulta (filtros.py) y el armado de cada panel como
ConsultaSemantica (paneles.py). El frontend no sabe de negocio: renderiza lo
que estos modulos devuelven."""
